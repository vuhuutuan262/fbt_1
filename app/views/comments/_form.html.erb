<div class="media wow fadeInLeft animated">
  <span class="comment-form">
    <%= form_for [commentable, @comment], remote: true do |f| %>
      <%= f.text_field :content %>
      <%= f.submit "Comment" %>
    <% end %>
  </span>
</div>
